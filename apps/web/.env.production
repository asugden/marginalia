# Empty: the SPA and worker share an origin (the worker serves both static
# assets and /api/*), so API calls go to /api/* on the same host. Override
# (e.g. for a separate API subdomain) by setting VITE_API_BASE in a
# .env.local for development or here for a different production layout.
VITE_API_BASE=
