// course-items module entry. Pages are imported via this barrel so the main
// router only needs one import path per route.
// See ./README.md for the shape and the completion seam it implements.

export { AssignPage } from "./components/AssignPage.js";
