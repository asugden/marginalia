// Single-tenant course id for v0.1/v0.2. v0.3 replaces this with a real
// course picker (the dashboard work) — until then every page operates inside
// the seeded demo course.
export const DEMO_COURSE = "course_demo";
